# Comments before the first block should be ignored.
server {
    # Inline documentation between directives.
    listen 8080;
    root www/default; # trailing comments should be ignored.

    location / {
        allowed_methods GET POST DELETE;
        index index.html;
    }
}
